defmodule SafedocWeb.Customer.Document.IndexerDataView do
  use SafedocWeb, :view

  # def container_documents do
  #   Archive.list_containers_documents |> Enum.map(fn container -> [key: container.code, value: container.id] end )
  # end
end
